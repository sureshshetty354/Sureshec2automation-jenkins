pipeline {
    agent any

    stages {

        stage('Checkout Code') {
            steps {
                echo 'Code checkout completed'
            }
        }

        stage('Build') {
            steps {
                echo 'Build started'
            }
        }

        stage('Docker Build') {
            steps {
                echo 'Docker build stage'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment completed'
            }
        }
    }
}
